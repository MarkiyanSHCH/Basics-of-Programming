#include <iostream> 
#include <string>
using namespace std;

string* explode(string s, char c, int*& arr, int index)//???????? ???????? ??????? ?? ???????? ?? ??????????? ?? ? ?????
{
    string buff = "";
    int numberspace = count(s.begin(), s.end(), ' ');//???????????? ????????? ???????? ??? ????????? ????????? ???? ? ???????
    arr[index] = numberspace + 1;
    string* v = new string[numberspace + 1];
    int counter = 0;
    for (int i = 0; i < s.length(); i++) {//???? ???? ????????? ?? ???????? ?? ??????? ????? ??? ??????????? ???? ????? ????????? ?? ??????
        char g = s[i];
        if (g != c)
            buff += g;
        else
            if (g == c && buff != "") {
                v[counter] = buff;
                counter++;
                buff = "";
            }


    }
    if (buff != "") {
        v[counter] = buff;
        counter++;
    }

    return v;

}


void func(string a, string del) {
    string tmp;
    for (int i = 0; i < a.size(); i++) {
        for (int j = 0; j < del.size(); j++) {
            if (a[i] != del[j]) {
                tmp += a[i];
            }
        }
    }

    for (int i = 0; i < tmp.size(); i++) {
        cout << tmp[i];
    }
}



int main()
{
    string a;
    getline(cin, a);
    string del;
    getline(cin, del);
    

    func(a, del);
}